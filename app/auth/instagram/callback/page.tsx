import { CallbackHandler } from '../../../components/ig-callbackHandler'

export default function InstagramCallbackPage() {
  return (
    <main className="min-h-screen flex flex-col items-center justify-center">
      <CallbackHandler />
    </main>
  )
}